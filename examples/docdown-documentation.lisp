(in-package :docdown)

(defdoc index :page
  (:docdown
   "There is no great way to des"))

(defdoc generate-html-page :function
  (:docdown
   (node :section nil (:title "Abstract")
    (:docdown
     

   (defdoc nil :section

	     
   "Given a page docnode , generates a sting of HTML"))